spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/starbucks;
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create  # create: table을 삭제하고 다시생성
    properties:
      hibernate:
#       show_sql: true  #system out? ?? ??
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

logging:
  level:
    org.hibernate.SQL: debug #??? ?? ??
    org.hibernate.orm.jdbc.bind: trace #?? ???? ???? ??? ?? ? ??.(?? ??? ?? ??) +p6spy ????? but ???? ?? ?? ???. (??????? ??? ??)
